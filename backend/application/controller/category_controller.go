// controller/category_controller.go
package controller

// CategoryController handles category-related operations
type CategoryController struct {
	// Add category service dependencies here when needed
}

// NewCategoryController creates a new category controller
func NewCategoryController() (*CategoryController, error) {
	return &CategoryController{}, nil
}

// All existing category handler functions will be moved here as methods
// For now, we keep the existing functions as they are
// and will refactor them to use this controller instance
