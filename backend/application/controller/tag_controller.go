// controller/tag_controller.go
package controller

// TagController handles tag-related operations
type TagController struct {
	// Add tag service dependencies here when needed
}

// NewTagController creates a new tag controller
func NewTagController() (*TagController, error) {
	return &TagController{}, nil
}

// All existing tag handler functions will be moved here as methods
// For now, we keep the existing functions as they are
// and will refactor them to use this controller instance
