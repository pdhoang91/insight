// utils/themeClasses.js
// Backward-compatible re-export. All theme logic now lives in utils/theme/.
export {
  themeClasses,
  combineClasses,
  getThemeClass,
  componentClasses,
  enhancedThemeClasses,
  default,
} from './theme/index';
