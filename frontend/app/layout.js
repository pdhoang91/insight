export const metadata = {
  title: 'Insight Blog',
  description: 'A modern blog platform for sharing insights and knowledge',
};

export default function RootLayout({ children }) {
  return children;
}
